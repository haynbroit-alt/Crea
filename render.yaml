services:
  - type: web
    name: ai-video-agent
    env: python
    region: frankfurt
    plan: free
    buildCommand: apt-get update -qq && apt-get install -y -qq ffmpeg fonts-dejavu-core && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips="*"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false

  - type: cron
    name: world-publish-shorts
    env: python
    region: frankfurt
    plan: free
    # 09:00 and 18:00 UTC → 2 YouTube Shorts per day
    schedule: "0 9,18 * * *"
    buildCommand: pip install requests
    startCommand: >
      python -c "
      import os, requests, sys;
      url = os.environ.get('APP_URL','').rstrip('/') + '/world/publish';
      r = requests.post(url, timeout=60);
      print(r.status_code, r.text[:200]);
      sys.exit(0 if r.status_code < 400 else 1)
      "
    envVars:
      - key: APP_URL
        sync: false
